---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: prefetchrequests.kueue.x-k8s.io
spec:
  group: kueue.x-k8s.io
  names:
    kind: PrefetchRequest
    listKind: PrefetchRequestList
    plural: prefetchrequests
    singular: prefetchrequest
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: AdmissionCheck is the Schema for the admissionchecks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrefetchConfigSpec defines the desired state of PrefetchConfig
            properties:
              datapath:
                description: |-
                  Datapath specifies the location of datasets to be
                  used when workload starts
                type: string
              parameters:
                additionalProperties:
                  description: Parameter is limited to 255 characters.
                  maxLength: 255
                  type: string
                description: |-
                  lppf: Parameters for prefetch state
                  Parameters contains all other parameters classes may require.
                maxProperties: 100
                type: object
            type: object
          status:
            description: AdmissionCheckStatus defines the observed state of AdmissionCheck
            properties:
              state:
                description: 'Specify prefetch status: New, Prefetching, Incomplete,
                  Completed, Failed'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
